# 0 remember basename, show basename
# 0 look at docs, we want to add support for arguments
# 0 let's write a module that will help with getting arguments from command line
if __name__ == '__main__': # 0 ->
    # pass # 0
    # 0 pass does nothing, its like {} in C
    print(sys.argv) # 1
